package com.trio.test;

import java.math.BigDecimal;
import java.math.BigInteger;
import java.text.DecimalFormat;
import java.text.NumberFormat;
import java.util.List;
import java.util.Locale;

import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;

import com.trio.bean.h100.HondaH100Stglobals;
import com.trio.facade.MasterFacade;

public class Test1 {
	
	public static void main (String [] args){
		
		ApplicationContext ac = new ClassPathXmlApplicationContext("applicationContext.xml");
		
		MasterFacade mf = (MasterFacade) ac.getBean("masterFacade");
		
		BigDecimal bd = new BigDecimal(300000);
		
		BigInteger bi = new BigInteger("300000");

		//NumberFormat formatter = NumberFormat.getInstance(new Locale("en_US"));
		
		//NumberFormat formatter = NumberFormat.getInstance(new Locale("id_ID"));
		
		NumberFormat formatter = NumberFormat.getInstance(new Locale("id", "ID"));

		//System.out.println(formatter.format(bd.longValue()));
		
		System.out.println(formatter.format(bi)); 
		
	}
	
	private static String pisahkanRibuan(double data)	{
	String string = Double.toString(data);
	int titik = string.indexOf('.');
	String pecahan = string.substring(titik);
	long bulat = new Double(123456789).longValue();

	string = Long.toString(bulat);
	//String = pisahkan(new StringBuffer (string));

	return(string);
	}
	
	private static String tesRibuan(String angka){
		
		return "";
	}
}
